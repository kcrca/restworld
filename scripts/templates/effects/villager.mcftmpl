<%namespace file="effect_base.mcftmpl" import="*"/>
<%namespace name="base"  file="../base.mcftmpl"/>

<%base:loop collection="${professions}" args="when,i,thing">
${when(i)} ${summon("villager", 0, "Profession:%d,NoAI:True" % i)}
</%base:loop>
