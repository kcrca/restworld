<%namespace import="*" file="job_sites_base.mcftmpl"/>

<%
    sites = (
	'Cauldron',
	'Water Cauldron',
	'Lava Cauldron',
	'Powder Snow Cauldron',
    )
    stages = {
	'Water Cauldron': list('level=%d' % t for t in range(1, 4)),
	'Powder Snow Cauldron': list('level=%d' % t for t in range(3, 0, -1)),
    }
%>

${job_sites(sites, stages)}
