<%namespace import="*" file="blocks_base.mcftmpl"/>
<% types = ("Grass Block", "Podzol", "Mycelium", "Dirt Path") %>
# Make sure the block above is air so it doesn't turn dirt path to dirt instantly.
setblock ~0 ~4 ~0 air
${blocks(types, sign=1)}
