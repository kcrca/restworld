<%namespace name="base" file="../base.mcftmpl"/>

<%base:loop collection="${range(0, 8)}" args="when, i, thing">\
${when(i)} data merge entity @e[tag=item_frame_as_block,limit=1] {ItemRotation:${i}}
</%base:loop>
