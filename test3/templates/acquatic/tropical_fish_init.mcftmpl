% for i in range(0, 12):
<% tag, variants = fishes[i] %>\
summon minecraft:tropical_fish ~${0 + i / 6} ~2.2 ~${int(i % 6)} {Tags:[acquatic,${tag}],Variant:${variants[0][0]},CustomName:${text(variants[0][1])},NoAI:True,Silent:True,Rotation:[90f,0f]}
% endfor
