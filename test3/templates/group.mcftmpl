%if which == "enter":
<%doc> Adding 0 to a score creates it if it doesn't exist </%doc>
%for v in sorted(vars):
scoreboard players add ${v} funcs 0
%endfor
%endif

%for f in funcs:
execute at @e[tag=${f}_home] run function v3:${room}/${f}_${which}
%endfor
