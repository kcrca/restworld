<%namespace name="base"  file="base.mcftmpl"/>
<% stones = (Item("Stone"), Item("Smooth Stone")) %>

<%base:loop collection="${stones}" args="when,i,item">
${when(i)} setblock ~ ~3 ~ minecraft:${item.id}
${when(i)} data merge block ~-1 ~2 ~ {Text2:${text(item.name)}}
</%base:loop>
