<%namespace file="effect_base.mcftmpl" import="*"/>\
<%namespace file="../base.mcftmpl" import="*"/>\
<% animals = (Thing("cow"), Thing("Pig"), Thing("Horse"), Thing("Llama"), Thing("Sheep"), Thing("Polar Bear")) %>

${increment(len(animals))}\
% for i, a in enumerate(animals):
${when(i)} summon ${a.full_id()} ~0 ~0 ~0 {NoAI:True,Silent:True}
% endfor
