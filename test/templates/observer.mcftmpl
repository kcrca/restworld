<%namespace name="base"  file="base.mcftmpl"/>

<%base:loop collection="${('true', 'false')}" args="when,i,thing">\
${when(i)} setblock ~ ~4 ~ minecraft:observer[powered=${thing},facing=west]
${when(i)} data merge block ~1 ~2 ~ {Text3:${text("(Powered)" if thing == "true" else "")}}
</%base:loop>
