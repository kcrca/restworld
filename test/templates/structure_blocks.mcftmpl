<%namespace name="base"  file="base.mcftmpl"/>

<%base:loop collection="${structure_blocks}" args="when,i,thing">
${when(i)} data merge block ~ ~3 ~ {mode:${thing.id}}
${when(i)} data merge block ~-1 ~2 ~ {Text2:"\"${thing.name}\""}
</%base:loop>
