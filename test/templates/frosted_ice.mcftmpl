<%namespace name="base"  file="base.mcftmpl"/>
<%
    blocks = [Thing("Water"), ]
    for i in range (0, 4):
	blocks.append(Thing("Frosted Ice", block_state="age=%d" % i))
%>

<%base:loop collection="${blocks}" args="when,i,thing">
${when(i)} setblock ~ ~3 ~ ${thing.full_id()}
</%base:loop>
