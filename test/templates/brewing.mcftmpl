<%namespace name="base"  file="base.mcftmpl"/>
<%
    bottles = (
	(),
	(0,),
	(1,),
	(2,),
	(2,0),
	(1,2),
	(0,1),
	(0,1,2),
    )
%>

<%base:loop collection="${bottles}" args="when,i,where">
<% first = True %>\
${when(i)} data merge block ~ ~3 ~ {Items:[\
% for p in where:
${"" if first else ","}{Slot:${p},id:potion,Count:1,tag:{Potion:water}}\
<% first = False %>\
% endfor
]}
</%base:loop>
